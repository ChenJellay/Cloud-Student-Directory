# Knowledge notes (V1)

V1 answers must be traceable to official CMU sources. This prototype keeps a
**static in-code corpus** in `src/gateway/knowledge.py` rather than a live crawl.

Production follow-ups (out of scope here):

- Daily ingest of approved Registrar / Hub / OIE-linked pages
- Citation of the exact source URL on every generated answer (already returned by `/ask`)
- Explicit refusal when retrieval confidence is too low
